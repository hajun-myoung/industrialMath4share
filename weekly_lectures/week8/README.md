# Class

## Midterm

|                    |    Score    |
| :----------------: | :---------: |
|      Average       | 44.8 of 108 |
|      Highest       |  96 of 108  |
| Lowest `estimated` |    0-10     |
| A Cut `estimated`  |    50-52    |
| B Cut `estimated`  |    34-36    |

## Class contents

- Reg exp
  - `.` `*` `+` `?` `|` `^ $` `[]` `\w` `\W` `\d` `\D` `\s` `\S`
  - `RegExpString.test` method
    - returns true of false
  - `String.match(regexp)` method
    - returns matched strings
  - `String.replace(regexp)` method
    - replace matched strings to custom
  - mail address testor
- Array, Localize: skipped
- Async Programming
  - HTTP Method
  - Asynchronos Image Loading
    - `img.onload = () => URL.revokeObjectURL(img.src);`
    - bootstrap style class
  - Responses code
  - test those
- HTML 실행
  - Web Server(Live Server)
  - JS File loading(line13, define script(match name as html code))
  - flow: local, remote, server, client
  - sciprt: relative address
- addEventListner
  - use `document.addEventListener('DOMContentLoaded', () => {})` for stability
- Bootstrap minimum (loading by link tag)
- Promise
  - pending, fulfilled, rejected
- Reason of using Promise
  - for waiting, asynchronos
  - using new keyword, implement simple promise
